/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  images: {
    domains: ['image.tmdb.org'], // ✅ TMDB resimleri için hostname ekledik
  },
};

export default nextConfig;
